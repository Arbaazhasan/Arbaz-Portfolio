# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Recipient Contact Email (where messages are delivered)
CONTACT_EMAIL=arbaazhasan.ah@gmail.com

# SMTP Settings
# For Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-character-app-password
# (Generate App Password at: Google Account -> Security -> 2-Step Verification -> App Passwords)

# For custom SMTP / Mailtrap / SendGrid:
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
